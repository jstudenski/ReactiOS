import React, { Component } from 'react';
import Header from '../../components/Header';

const Calendar = () => (
  <div>
    <Header
      title="Calendar"
      secondary
      search
    />
  </div>
);

export default Calendar;
